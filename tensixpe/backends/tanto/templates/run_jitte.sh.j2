#!/usr/bin/env bash
set -euo pipefail

echo "[run_jitte] project: {{ project_name }}"
echo "[run_jitte] ronin_home: {{ ronin_home }}"
echo "[run_jitte] TODO: export TT_METAL_HOME=\$RONIN/jitte/home, TT_ARCH=wormhole_b0, then run the built host binary"
exit 0
