#!/usr/bin/env bash
set -euo pipefail

echo "[build] project: {{ project_name }}"
echo "[build] ronin_home: {{ ronin_home }}"
echo "[build] TODO: invoke real Ronin/Tanto build here (e.g. tanto build host.cpp kernels/*.cpp)"
exit 0
